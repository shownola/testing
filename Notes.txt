Adding guard to app:
  Guard keeps the testing running in the background

Add these gems in development:
  gem 'guard'
  gem 'guard-rspec'
  gem 'guard-cucumber'

$ bundle install
$ bundle binstubs guard
$ guard init
